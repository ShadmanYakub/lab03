<% for (let i = 1; i <= Math.ceil(total / perPage); i++) { %>
    <% if(i === page) { %>
      <span><%= i %></span>
    <% } else { %>
      <a href="/bookings/paginate?page=<%= i %>&perPage=<%= perPage %>"><%= i %></a>
    <% } %>
  <% } %>